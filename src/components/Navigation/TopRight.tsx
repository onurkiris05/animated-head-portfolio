import styled from "styled-components";

const Container = styled.div`
  position: absolute;
  top: 1rem;
  right: 1rem;
`;

function TopRight() {
  return <Container>TopRight</Container>;
}

export default TopRight;

import styled from "styled-components";

const Container = styled.div`
  position: absolute;
  bottom: 1rem;
  left: 1rem;
`;

function BottomLeft() {
  return <Container>BottomLeft</Container>;
}

export default BottomLeft;
